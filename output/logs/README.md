This folder should contain the `.npz` log files generated by diffusion and MPC controllers.

Each log file contains different fields depending on the controller used for generating the logs.
However, core fields like `actions` are shared between controllers and unique fields are only used for additional information (like all samples for batched diffusion sampling or slack variables for MPC).